


<style>
* {
  box-sizing: border-box;
}

body {
  font-family: Arial, Helvetica, sans-serif;
}

/* Style the header */
header {
  background-color:#1f5898a3;
  padding: 30px;
  text-align: center;
  font-size: 35px;
  color: white;
}
.selectionAffichage{
        font-size: 15px;
    font-style: oblique;
}
.container {
    width: 1170px;
    height: 900px;
}
/* Create two columns/boxes that floats next to each other */
form {    height: 838px;
  float: left;
  width: 30%;
height: 814px; /* only for demonstration, should be removed */
  background: #ccc;
  padding: 20px;
}
#pdf_file2 {
margin: -1px;
height: 815px;
}
  #pdf_file1 {
margin: -1px;
height: 815px;
    
}
/* Style the list inside the menu */
.nav-tabs>li>a {
    margin-right: 2px;
    line-height: 1.42857143;
    border: 1px solid transparent;
    border-radius: 4px 4px 0 0;
    font-size: 24px;
}
article {height: 814px;
  float: left;
  padding: 0px;
  width: 70%;
  background-color: #f1f1f1;
     height: 814px; /* only for demonstration, should be removed */
}

#Partager {
        margin-top: 0px;
}


</style>
<script>


    'use strict';
 
    document.addEventListener( "DOMContentLoaded", ev => {
      
    }, false );
    
    window.addEventListener( "load", ev => {
    
      document.querySelector( "#selectionAffichage" ).addEventListener( "change", ev => {
        let n = ev.target.value;
        console.log("testtttttt",ev.target)
        
        console.log("doci",document.querySelector('#pdf_file').src = 'http://{{domain}}/media/files/'+n)
        console.log("doci",document.querySelector('#pdf_file1').src = 'http://{{domain}}/media/files/'+n)
        console.log("doci",document.querySelector('#pdf_file2').src = 'http://{{domain}}/media/files/'+n)

        document.querySelector('#inputFile').value = n;
        document.querySelector('#inputFileS').value = n;
        console.log("doc",document.querySelector('#inputFile').value)
      });
   document.querySelector( "#selectionPartage" ).addEventListener( "change", ev => {
        let n = ev.target.value;
        console.log("doci",document.querySelector('#pdf_file').src = 'http://{{domain}}/media/files/'+n)
        console.log("doci",document.querySelector('#pdf_file1').src = 'http://{{domain}}/media/files/'+n)
        console.log("doci",document.querySelector('#pdf_file2').src = 'http://{{domain}}/media/files/'+n)

      });
      //kIDUnique();
    }, false );
  </script>
  
<header>
 
<ul class="nav nav-tabs" id="myTab" role="tablist">
  <li class="nav-item">
    <a class="nav-link active" id="afficher-tab" data-toggle="tab" href="#afficher" role="tab" aria-controls="afficher" aria-selected="true">Afficher</a>
  </li>
  <li class="nav-item">
    <a class="nav-link" id="profile-tab" data-toggle="tab" href="#profile" role="tab" aria-controls="profile" aria-selected="false">Supprimer</a>
  </li>
  <li class="nav-item">
    <a class="nav-link" id="Partager-tab" data-toggle="tab" href="#Partager" role="tab" aria-controls="Partager" aria-selected="false">Partager</a>
  </li>
</ul>


</header>
<div class="jumbotronn">
<section> {{array.[0].item}}
    <article>
<div class="tab-content" id="myTabContent">
  <div class="tab-pane fade active" id="afficher" name="afficher" role="tabpanel" aria-labelledby="afficher-tab" >
     <embed  id="pdf_file" src="http://{{domain}}/media/files/{{dataA.[0].nomRessource}}" width=800 height=813 type='application/pdf'/>
  
  </div>



  <div class="tab-pane fade" id="profile" role="tabpanel" aria-labelledby="profile-tab">

<form class="form-signin" action="/Media/supprimer" method="POST">
  <h2 class="form-signin-heading">Supprimer un fichier</h2>

<p>Selectionner un fichier à supprimer à droite</p>
<input type="text" id="inputFileS" name="selectionDelete" class="form-control" placeholder="Selectionne un fichier a gauche" required autofocus>
  <br/>
  
  <button class="btn btn-lg btn-default btn-block" type="submit">Supprimer</button>
</form>
 <embed  id="pdf_file1" src="http://{{domain}}/media/files/{{dataA.[0].nomRessource}}" width=559 height=657 type='application/pdf'/>
  
      
  </div>
  
  <div class="tab-pane fade " id="Partager" role="tabpanel" aria-labelledby="Partager-tab">
   
 <form class="form-signin" action="/Media/partage" method="POST">
  <h2 class="form-signin-heading">Partager un fichier</h2>

  <label for="inputEmail" class="sr-only">Email address</label>
  <input type="email" id="inputEmail" name="email" class="form-control" placeholder="name@example.com" required autofocus>
<p>Selectionner un fichier a partager a droite</p>
<input type="text" id="inputFile" name="selectionAffichage" class="form-control" placeholder="Selectionne un fichier a gauche" required autofocus>
  <br/>
  
  <button class="btn btn-lg btn-default btn-block" type="submit">Partager</button>
</form>

       <embed  id="pdf_file2" src="http://{{domain}}/media/files/{{dataA.[0].nomRessource}}" width=559 height=657 type='application/pdf'/>
  
  </div>
</div>
  </article>
    <div>
  <form>
 
  <div class="form-group">
    <label for="selectionAffichage" class="selectionAffichage">Mes fichiers</label>
    {{#if dataA}}
    <select multiple class="form-control" id="selectionAffichage" >
      {{#each dataA}}
            <option >{{nomRessource}}</option>
        {{/each}}
    </select>
    {{/if}}
  </div>

    <div class="form-group">
    <label for="selectionPartage" class="selectionAffichage">Les fichiers partagés avec moi </label>
    {{#if dataB}}
    <select multiple class="form-control" id="selectionPartage" >
      {{#each dataB}}
            <option >{{nomFile}}</option>
        {{/each}}
    </select>
    {{/if}}
  </div>

  </div>
</form>
 
</section>
</div>
<div>